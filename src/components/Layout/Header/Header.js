import React from "react";
import { SHeader, SHeaderContent, SLogo } from "./styles";

const Header = () => {
    return (
        <SHeader>
            <SHeaderContent>
                <SLogo>LibreView</SLogo>
            </SHeaderContent>
        </SHeader>
    );
};

export default Header;
